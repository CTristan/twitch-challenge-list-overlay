/**
 * Configuration type enum for admin panel UI updates
 * Used to specify which type of configuration is being updated
 */
export enum ConfigType {
    COLOR = "color",
    BACKGROUND = "background",
    BEHAVIOR = "behavior",
}
